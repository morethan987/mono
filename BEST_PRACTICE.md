# 壹刻 (mono) 最佳实践指南

> 从"管理任务清单"转向"拥有智能秘书"。

`mono` 的设计哲学是：**用户负责定义目标，系统负责规划路径。** 本指南将帮助你发挥 `mono` 进化后的全部潜力。

---

## 💡 核心哲学：秘书化思维

传统的 Todo 软件要求你不断维护优先级、截止日期和复杂的标签。在 `mono` 中，你应该：
1. **信任推断**：大多数时候，你只需要输入任务标题。
2. **专注当下**：只关注 `mono now` 推荐的那一件事。
3. **真实反馈**：不管是完成、推迟还是被中断，真实地记录行为，系统会越来越懂你。

---

## 🛠️ 高效工作流

### 1. 晨间/晚间批量录入 (`mono add`)
不要在工作高峰期频繁录入任务。建议在每天开始前或前一天晚上，将大脑中的想法快速录入。
- **极简模式**：`mono add "完成 PPT 初稿"` （系统会自动推断时长和优先级）。
- **智能增强**：如果你觉得系统需要更多线索，使用 `--infer`。
- **建立关联**：如果你在规划一个大项目，先创建一个主任务，然后使用 `mono spawn` 细化。

### 2. 进入专注状态 (`mono now`)
这是 `mono` 的核心入口。
- 运行 `mono now`。你会看到系统认为你现在**最应该做**的一件事。
- 如果你不想做这一件，运行 `mono now --queue` 查看备选的前 5 项。
- 确定后，运行 `mono start <id>`（或直接从通知窗口点击）。

### 3. 处理动态变化 (`mono spawn`)
在执行任务 A 的过程中，如果突然产生了一个相关的子任务 B：
- 不要跳出当前上下文。
- 运行 `mono spawn "处理 A 过程中的遗留问题"`。
- 该任务会自动关联到 A，并在 A 完成后被系统纳入未来的调度建议中。

### 4. 环境感知与自动驾驶
如果你使用的是 **niri 窗口管理器**，`mono` 的表现将如虎添翼：
- **上下文识别**：如果你正在写代码（IDE 窗口），系统会识别你在进行 `work`。
- **中断学习**：如果你在工作任务期间切换到社交软件或娱乐网站超过 45 秒，`mono` 会自动记录一次中断。
- **无需手动**：你不需要频繁告诉系统你"分心了"，它会默默学习并在下次调度时避开容易让你分心的时段。

---

## 📊 深度复盘 (`mono stats`)

每周运行一次 `mono stats -v`：
- **查看贝叶斯时长预测**：看看系统预测的 `45±10m` 是否符合你的体感。
- **分析中断模式**：看看哪些时段你的"已中断"次数最高，考虑在那段时间安排更轻松的任务。
- **观察进化**：随着样本增加，你会发现系统推荐的准确度稳步提升。

---

## 🔧 高级配置建议

### Niri 集成配置
确保你的环境变量中有 `NIRI_SOCKET`。`mono` 会自动通过该 socket 与 compositor 通信。

### 自动完成父任务
默认情况下，当所有子任务标记为 `Completed` 时，父任务会自动完成。如果你需要手动控制，可以在配置中关闭 `auto_complete_parent`。

### 时间衰减
`mono` 默认开启了每小时一次的时间衰减（因子 0.999）。这意味着你一个月前的习惯对现在的影响只有约 50%。这让 `mono` 能够快速适配你新的生活节奏（如入职新公司或开学）。

---

> **记住**：`mono` 越用越聪明。不要害怕"弄乱"它的模型，你的每一个真实行为都是它进化的养料。
